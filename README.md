# OptiImage Local

Offline, privacy-first image compressor/resizer/converter.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies: `npm install`
2. Run the app: `npm run dev`

## Static Build (Deploy)

- Build static site: `npm run build`
- Output folder: `out/`
- Preview the static output locally: `npm run preview`
